#include <iostream>
#include "algorithms/logs/Log.h"
#include "modeling.h"
#include "modeling/models/ObjectStore.h"
#include "modeling/variables/VarAttributes.h"

int main() {
/*
    ObjectStore<Var, VarAttributes> variables;

    Model model;
    auto x = model.add_var(0., 1., Continuous, 0.);

    variables.add(x, 0., 1., Continuous, Column(0.));

    std::cout << variables.attributes(x).lb() << std::endl;
    std::cout << variables.attributes(x).ub() << std::endl;

    for (const auto& var : variables.objects()) {
        std::cout << variables.attributes(var).id() << std::endl;
    }

    variables.remove(x);

    auto y = model.add_var(0., 1., Continuous, 0.);
    variables.add(y, 0., 1., Continuous, Column(0.));

    std::cout << variables.has(x) << std::endl;
    std::cout << variables.has(y) << std::endl;
*/

    return 0;
}

